<h1>Welcome, <%= current_user.username %></h1>

<h2>All Added Extinct Animals</h2>
<ol>
    <% Animal.all.each do |animal| %>
        <li id="animal-added-by"><a href="/animals/<%= animal.id %>" id="extinct-animal"><%= animal.name %></a> added by <%= animal.user[:username] %></li>
    <% end %>
</ol><br>

<form action="/animals/new" method="get">
    <button class="button" type="submit">Add Another?</button>
</form>